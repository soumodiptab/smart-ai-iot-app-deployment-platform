// MongoDB Playground
// Use Ctrl+Space inside a snippet or a string literal to trigger completions.
const database = 'platform_db';
const collection = 'services';

// Create a new database.
use(database);

// Create a new collection.
db.createCollection(collection);
// Insert entries in collection
db.services.insertOne({service: "ai_manager",docker:"enabled",repo_loc:"ai_manager"});
db.services.insertOne({service: "app_manager",docker:"enabled",repo_loc:"app_manager"});